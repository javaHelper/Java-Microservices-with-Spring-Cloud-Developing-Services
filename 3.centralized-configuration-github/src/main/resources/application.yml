server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javaHelper/pluralsight-spring-cloudconfig-wa-tolls
          default-label: main
          search-paths:
            - station*